# Runtime dependencies (production).
# Compile via: pip-compile --generate-hashes --output-file=requirements.txt requirements.in

fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
aiosqlite>=0.20,<1.0
pydantic-settings>=2.4,<3.0
