# client--francesco-ping-app

> Fremd-Repo von Francesco. SSOT ist `origin` (git@github.com:sgm9p6bp4v-prog/ping-app.git).
> Wir liefern zusaetzlichen Code zu, kein Deployment auf unserer Seite.

## Global Rules
@~/.claude/CLAUDE.md

## Project-Specific

### Context
- `00_infos/llm-context.md` — Projekt-Kontext (sobald Code existiert)
- `00_infos/repo-contract.yaml` — maschinenlesbarer Vertrag
- `00_infos/meta/open-questions.md` — HOLDs

### Overrides
- **SSOT:** Francescos `origin`. Direktes Pushen nach `origin` ist erlaubt — aber nur auf **ausdruecklichen User-Befehl** (autorisiert 2026-05-25). Kein autonomer Push.
- **Kein Deploy** in unserer Fleet. Keine Woodpecker-Pipeline, kein Edge-Stack.
- **Branch-Konvention:** unsere Beitraege auf `feat/varga-*` oder `fix/varga-*` (klar als extern markiert). Pushes nach `main` weiterhin nur via PR.
